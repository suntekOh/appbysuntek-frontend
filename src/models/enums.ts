export enum AppBarType {
    Annonymous,
    Authenticated
}

export enum AppBarMenu {
    Default,
    QnA,
    SignIn,
    SignOut
}

export enum HomeNavMenu {
    None,
    SignOut,
}

export enum VisibleCondition {
    Always,
    Annonymous,
    Authenticated
}

